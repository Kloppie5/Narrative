using System;
using System.IO;

namespace SaveFileEditor {
    class SaveFileEditor {
        protected String filePath;

        public SaveFileEditor(String filePath) {
            this.filePath = filePath;
        }
    }
}
